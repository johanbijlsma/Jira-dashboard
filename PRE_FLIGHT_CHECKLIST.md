# Pre-flight checklist

- `composer install` en `npm install` succesvol uitgevoerd
- `.env` gevuld met MySQL-, Jira- en Teams-config
- `php artisan migrate` succesvol
- `php artisan test` groen
- `npm run build` succesvol
- queue worker en scheduler ingericht
