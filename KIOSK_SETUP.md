# Kiosk setup

Gebruik voor kioskgebruik alleen de Laravel-webapp:

1. Deploy de applicatie op een vaste URL.
2. Start een browser in fullscreen op `/`.
3. Gebruik een read-only account of netwerksegment als de kiosk geen beheeracties mag uitvoeren.
